# 火柴人插帧 — Tweener 的预备项目

给 [Tweener](./TALK.md)（Krita 自动中割插件）做的降维版本：
先在火柴人上把「前后两帧 → 中间帧」这件事从头做一遍，再回去做真线稿。

**这不是一次性玩具。** 数据格式、评测脚本、目录结构全部与主项目对齐，
写出来的东西会直接变成主项目的第一个 backend。

---

## 为什么先做这个

主项目那三条「为什么难」里：


| 难点            | 火柴人版本                                |
| ------------- | ------------------------------------ |
| 线稿几乎没纹理，光流不适定 | **删掉**。表示就是 17 个关节 × (x, y)，对应关系天生免费 |
| 帧间位移远大于视频     | **保留**。这是唯一留下的变量                     |
| 中间帧涉及拓扑变化     | **删掉**。骨架固定，不会有线条断开合并                |


好的简化不是「同一个问题做小一点」，而是**只留一个变量**。
剩下的这个变量——大位移下的运动建模——正是问题的核心。

学名叫 **motion in-betweening**，是个成熟子领域，
奠基工作是 Ubisoft La Forge 的 Robust Motion In-betweening（SIGGRAPH 2020，LaFAN1 数据集）。
不复现他们的仓库（那是 3D 的，BVH + 四元数 + FK + 脚部接触，
一整套跟二维线稿无关的包袱），但**偷他们的评测协议**：
零速度和线性插值必须作为基线上报，按过渡长度分档看。

---



## 运行环境

- conda 环境 `ai`：`~/Desktop/YJH_Data/Projects/Quant/yes/envs/ai/bin/python`
- Python 3.11.15，torch 2.5.1+cu121，torchvision 0.20.1
- GPU：RTX 4090 24GB（这个项目其实 CPU 就够，训练是秒级）
- **v0 只依赖 numpy 和 Pillow**，不需要 torch

---



## 目录

```
Tweener/
├── stickman/
│   ├── skeleton.py      关节顺序、骨骼连接、渲染、指标        [已完成]
│   ├── motion.py        程序化动作生成（固定骨长 + 关节角 + FK）[已完成]
│   ├── baselines.py     zero_velocity / lerp / angle_lerp     [已完成]
│   ├── dataset.py       torch Dataset                          [v2 加]
│   └── models/          mlp.py / seq.py / unet.py              [v2+ 加]
├── make_triplets.py     造三连帧                                [已完成]
├── extract_poses.py     MediaPipe 从视频提关键点                [v1 加]
├── train.py                                                     [v2 加]
├── evaluate.py          跑各方法，出对比图 + 指标表              [已完成]
├── data/triplets/       <片段名>/{prev,gt,next}.png + meta.json
├── runs/<时间戳>/       config.json / metrics.csv / summary.txt / grid_*.png
└── server/              主项目的模型服务，v5 才动
```

---



## 约定（定了就别改）

**关节顺序**：COCO-17（nose, left_eye, right_eye, left_ear, right_ear,
left/right shoulder, elbow, wrist, hip, knee, ankle）。
选它的唯一理由是 MediaPipe / RTMPose / OpenPose 都能直接映射，
将来换真人视频提取的姿态不用改一行下游代码。
写在 `skeleton.py:JOINTS`，是唯一真相来源。

**坐标**：归一化到 [0, 1]，原点左上，y 向下（跟图像坐标系一致）。
渲染时乘画布边长。默认画布 256。

**角度**：从 +x 轴起算，因为 y 向下，所以 `-pi/2` 是正上方，`+pi/2` 是正下方。

**三连帧目录**：

```
data/triplets/<片段名>/
    prev.png  gt.png  next.png
    meta.json   {motion, kind, stride, size, poses{prev,gt,next}, params{...}}
```

`meta.json` 里的 `poses` 是 v0 的**免费真值**——渲染时顺手存下来的，
不需要从 PNG 里检测。等你想加难度就把它删掉，逼自己写关键点检测。
那一步就是 AnimeInbet 里「栅格线稿 → 向量图」的玩具版，
也正是那篇论文在真实数据上卡住的地方。

**运动类型标注**：`small_local` / `medium_cyclic` / `large_rotation` /
`large_translation` / `global_rotation`。评测按类型分组看。

---



## v0 现状：已完成

三个基线，零模型。实测结果（stride=6，60 组三连帧，单位像素）：


| 运动类型                  | zero_velocity | lerp                | angle_lerp |
| --------------------- | ------------- | ------------------- | ---------- |
| small_local（挥手）       | 0.60          | 0.45                | 0.43       |
| large_rotation（抡臂）    | 4.07          | 1.72（骨长误差 **2.40**） | **0.00**   |
| medium_cyclic（走路）     | 10.47         | 8.22                | 7.78       |
| large_translation（下蹲） | 11.88         | 8.80                | 8.05       |
| global_rotation（侧倾）   | 17.29         | 12.82               | 12.29      |


**三条结论，后面所有决策都建立在它们之上：**

1. **抡臂那行 angle_lerp = 0.00。** 那个动作在角度空间里线性，懂结构就等于免费拿到答案。
  而 lerp 的骨长误差 2.40 像素——关节沿弦而不是沿弧移动，胳膊被插短了，
   叠图里能看到红线切进身体。**这是几何路线（AnimeInbet）存在的全部理由。**
2. **走路和侧倾两行，lerp 和 angle_lerp 几乎一样烂**（8.22 vs 7.78）。
  瓶颈不在几何，懂结构也救不了。瓶颈在**时间分配**：
   真实动作有缓动，而两种插值在时间上都是线性的。
3. **侧倾那格是题眼。** prev 和 next 都站得笔直，gt 却歪到极限，
  因为它落在正弦周期的四分之一处。**任何只看两帧的方法都不可能做对。**
   这是「两帧信息量不足」的可视化证据，也是 v3 换成多帧上下文的直接动机。

**已知局限**：`motion.py` 的动作是正弦函数造的，所以结论 2 里
「时间上非线性」有一部分是造数据方式带来的。
真实动作确实有缓动，但强度不一定长这样。
**这批数据只用来验证管线，不要拿来训练。**

---



## 实现顺序



### v1 — 换成真实数据

`extract_poses.py`：MediaPipe Pose 跑几段视频（走路、挥手、转身、跳），
33 个关键点映射到 COCO-17，写成跟现在一样的三连帧格式。

- 关键点会抖、会丢帧、会左右手互换，清洗逻辑本身就是收获
- 至少 5 类运动、每类 30 组以上，运动类型照旧标注
- 覆盖不同 stride（2 / 4 / 8），因为方法在短过渡和长过渡上表现完全不同
- **验收**：三个基线在真实数据上重跑，得到一张新的结论表。
跟 v0 的表对比——哪些结论站得住，哪些是合成数据的假象



### v2 — 最小可训练模型

输入 68 维（前后两帧各 17×2），输出 34 维。两三层 MLP，规模跟 VNumber 差不多。

- **做好打不过 lerp 的心理准备。** LaFAN1 那套基准里线性插值就是很强的基线
- **验收**：在 `evaluate.py` 的 `METHODS` 里注册，跟三个基线同表对比。
打不过要如实记录，并写清楚为什么——多半就是上面的结论 3
- 一个有用的中间试验：让模型预测**残差**（相对 lerp 的偏移量）而不是绝对坐标。
相当于把 lerp 当先验送进去，通常立刻就能超过它



#### v2 实测结果（真实独立验证视频）

模型使用前后两帧 COCO-17 坐标作为 68 维输入，预测相对于
线性插值的 34 维残差。每个 stride 单独训练一个两层 MLP。


| stride | lerp MPJPE | MLP MPJPE | 改善    | lerp 骨长误差 | MLP 骨长误差 | 改善    |
| ------ | ---------- | --------- | ----- | --------- | -------- | ----- |
| 2      | 0.4782     | 0.4751    | 0.65% | 0.3461    | 0.3364   | 2.80% |
| 4      | 0.9630     | 0.9234    | 4.12% | 0.6206    | 0.5855   | 5.65% |
| 8      | 2.1675     | 1.9514    | 9.97% | 1.1023    | 1.0617   | 3.68% |


主要结论：

1. 短间隔上 lerp 已经非常强，MLP 几乎没有优势。
2. 间隔越大，残差 MLP 的位置优势越明显。
3. s8 的 swing MPJPE 改善 16.5%，骨长误差改善 42.6%。
4. s8 的 squat MPJPE 改善 33.1%，但只有 11 个验证样本。
5. wave s8 明确失败：MPJPE 恶化 8.6%，骨长误差恶化 7.6%。
6. MLP 主要修正高难度样本，并非在多数样本上都优于 lerp。

骨长损失消融（s8）：


| bone_weight | MPJPE  | 骨长误差   |
| ----------- | ------ | ------ |
| 0.0         | 1.9151 | 1.1165 |
| 0.5         | 1.9511 | 1.0615 |


骨长约束牺牲约 1.9% MPJPE，换来约 4.9% 的骨长误差改善；
收益主要集中在大幅旋转动作。说明位置准确和结构稳定是两个
不能被单一指标替代的目标。

### v3 — 序列模型

改成「给前 k 帧和后 k 帧，补 N 帧」，小 Transformer。
加 time-to-arrival embedding（Harvey 2020 的做法：一个让单个模型
支持变长过渡的时序嵌入）。

- **验收**：在 v0 结论 3 那类样本（两帧不足以确定中间姿态）上，
多帧上下文有没有把它救回来。这是这一步唯一要回答的问题



#### v3 实测结果（多帧上下文 Transformer）

模型使用目标帧左右各 3 帧，共 6 个 COCO-17 姿态 token，
加入连续 time-to-arrival embedding。一个共享 Transformer
同时训练 stride 2、4、8，并预测相对于端点 lerp 的姿态残差。

验证集使用 5 段独立验证视频，共 1188 个序列样本。


| stride | lerp MPJPE | MLP MPJPE | Transformer MPJPE | 相对 lerp | 相对 MLP |
| ------ | ---------- | --------- | ----------------- | ------- | ------ |
| 2      | 0.4435     | 0.4408    | 0.4459            | -0.54%  | -1.17% |
| 4      | 0.9415     | 0.9041    | 0.8836            | +6.14%  | +2.27% |
| 8      | 2.0698     | 1.8531    | 1.7687            | +14.55% | +4.55% |



| stride | lerp 骨长误差 | MLP 骨长误差 | Transformer 骨长误差 | 相对 lerp |
| ------ | --------- | -------- | ---------------- | ------- |
| 2      | 0.3212    | 0.3118   | 0.3159           | +1.65%  |
| 4      | 0.6013    | 0.5689   | 0.5677           | +5.60%  |
| 8      | 1.0548    | 1.0234   | 0.9388           | +11.00% |


全部样本自然加权时，Transformer 相对 lerp 的 MPJPE 改善
6.79%，骨长误差改善 5.59%。三个 stride 宏平均时，MPJPE
改善 10.32%，骨长误差改善 7.84%。

主要结论：

1. 多帧上下文的收益随插帧间隔增加，s8 的收益最明显。
2. Transformer 相对 MLP 在 s8 上进一步改善 4.55% MPJPE
  和 8.27% 骨长误差。
3. 大幅平移和大幅旋转动作中，可以直观看到多帧上下文
  修正了两端点无法确定的中间动作。
4. s2 上 lerp 和 MLP 已经足够强，Transformer 没有优势。
5. small_local 动作中，MLP 和 Transformer 都可能输给 lerp；
  多帧模型不能无条件替代简单基线。
6. 最差案例显示 Transformer 会发生过度修正和动作阶段判断错误，
  尤其可能把身体倾斜预测到错误方向。
7. 坐标模型输出的火柴人线条始终清晰，主要失效模式是姿态错误、
  全局位移错误和肢体角度错误，而不是图像模糊。

因此 v3 的唯一验收问题可以回答为：多帧上下文确实能救回一部分
两帧信息不足的困难样本，但收益集中在长间隔和大幅非线性动作，
不是所有动作上的统一提升。

### v4 — 栅格对照实验（**最值钱的一步**）

把 v3 的关节坐标渲染成 PNG，训一个小 UNet 做同样的任务：
两张火柴人图片进，中间那张图片出。同一批数据、同一套评测。

- 预期看到：几何表示线条干净但姿势可能离谱，栅格表示姿势大致对但线条糊
- **这就是 AnimeInbet 和 RIFE 的核心分歧**，用火柴人一天跑出来
- **验收**：两条路线的对比图并排放，能一眼说清各自的失效模式。
跑完这一步再去读那些论文，读到的就不是名词了



#### v4 正式实现

为了与 v3 进行公平对照，正式实现没有只输入前后两张图片，
而是使用与 Transformer 完全相同的左右各 3 帧上下文。

每个样本包含：

- 6 张 256×256 火柴人前景图
- 6 张对应的 time-to-arrival 常数图
- 合计 12 个输入通道
- 1 张目标前景图

RasterUNet 使用 16、32、64、128 四级通道，小型编码器—解码器
结构，共 489297 个参数。模型预测相对于 pixel LERP logits 的
残差，输出层使用零初始化，因此初始状态近似等于 pixel LERP。

训练损失包含：

1. 稀疏前景加权 BCE
2. Dice loss
3. 允许 1、2、4 像素偏移的双向线条距离损失

正式训练和验证继续使用 v3 的同一批数据：

- 训练序列：1588
- 独立验证序列：1188
- stride：2、4、8
- 五种运动类型全部覆盖

正式运行目录：

`runs/formal_raster/raster_unet_k3/20260826_164507`

统一评测目录：

`runs/formal_raster_eval/20260826_172117`

#### v4 统一图像评测结果

所有坐标预测均使用同一个 renderer 渲染为 256×256 前景图，
再与栅格方法使用完全相同的 Dice、IoU、MAE 和线条距离指标。
最终二值线稿以阈值 0.5 的 hard Dice 作为主要视觉指标。


| 方法          | s2 hard Dice | s4 hard Dice | s8 hard Dice | 自然加权       | stride 宏平均 |
| ----------- | ------------ | ------------ | ------------ | ---------- | ---------- |
| pixel LERP  | 0.8192       | 0.7157       | 0.5957       | 0.7609     | 0.7102     |
| pose LERP   | **0.9239**   | 0.8606       | 0.7298       | 0.8808     | 0.8381     |
| Transformer | 0.9216       | **0.8653**   | **0.7577**   | **0.8844** | **0.8482** |
| Raster best | 0.8678       | 0.8067       | 0.6996       | 0.8288     | 0.7914     |
| Raster last | 0.8824       | 0.8282       | 0.7268       | 0.8469     | 0.8125     |


主要结果：

1. pixel LERP 始终最差，说明直接平均两张图会产生双头、
  双躯干和灰色重影，无法得到真正的中间线稿。
2. s2 中 pose LERP 最好，短间隔没有必要使用复杂模型。
3. Transformer 在 s4、s8 和宏平均上最好，证明多帧上下文
  对长间隔非线性动作确实有效。
4. Raster last 明显优于 pixel LERP，说明 UNet 学到了有效的
  中间位置和轮廓修正，不是在复制输入。
5. Raster last 在 s8 上接近 pose LERP，但整体仍未超过坐标
  Transformer。
6. 栅格路线和坐标路线具有互补失效模式，但当前最可靠的单一路线
  仍然是多帧坐标 Transformer。



#### Raster best 与 last 的选择冲突

训练按照综合验证损失保存的 `best.pt` 来自 epoch 7：

- 最佳验证宏平均损失：0.459978

训练最终的 `last.pt` 来自 epoch 47。虽然它的综合验证损失上升到
约 0.8290，但 Dice、IoU、MAE 和视觉线条质量普遍更好。


| stride | best hard Dice | last hard Dice | last 单样本胜率 |
| ------ | -------------- | -------------- | ---------- |
| s2     | 0.8678         | 0.8824         | 82.1%      |
| s4     | 0.8067         | 0.8282         | 81.5%      |
| s8     | 0.6996         | 0.7268         | 76.0%      |


原因是后期预测变得更细、更锐利。少量一像素错位会受到加权 BCE
的强烈惩罚，因此综合损失变差；但模糊、双轮廓和阈值碎点反而减少。

当前模型管理结论：

- `last.pt`：视觉展示和后续接口的 Raster 候选
- `best.pt`：最低综合损失 checkpoint，保留用于消融
- 两个 checkpoint 都不能删除

后续若重新训练，应分别保存：

- `best_total_loss.pt`
- `best_hard_dice.pt`
- `last.pt`



#### v4 视觉失效模式

路线对照图：

- `runs/formal_raster_eval/20260826_172117/selection_grids/route_comparison_s2.png`
- `runs/formal_raster_eval/20260826_172117/selection_grids/route_comparison_s4.png`
- `runs/formal_raster_eval/20260826_172117/selection_grids/route_comparison_s8.png`

checkpoint 对照图：

- `runs/formal_raster_eval/20260826_172117/selection_grids/checkpoint_comparison_s2.png`
- `runs/formal_raster_eval/20260826_172117/selection_grids/checkpoint_comparison_s4.png`
- `runs/formal_raster_eval/20260826_172117/selection_grids/checkpoint_comparison_s8.png`

代表性 s8 对照：

![v4 route comparison s8](runs/formal_raster_eval/20260826_172117/selection_grids/route_comparison_s8.png)

![v4 checkpoint comparison s8](runs/formal_raster_eval/20260826_172117/selection_grids/checkpoint_comparison_s8.png)

视觉结论：

1. 坐标方法的线条和骨架连接始终清晰，但失败时会产生一个
  结构完整、动作阶段却错误的火柴人。
2. Transformer 可能判断错倾斜方向、动作阶段或全局位置，
  但不会产生图像碎片。
3. Raster UNet 有时比 Transformer 更接近正确的整体位置，
  尤其在部分大幅平移和全局旋转案例中。
4. Raster 的主要失败是头部圆弧缺失、手臂断开、双轮廓、
  粗线和局部结构消失。
5. epoch 7 的输出更模糊并保留多个可能轮廓；epoch 47 通常将其
  收缩成一条更清晰的线，但少数案例会过度删除正确像素。
6. `walk_val_001_*_0496` 是一个稳定异常案例：epoch 7 虽然模糊，
  但比 epoch 47 覆盖更多真值；两个模型在该片段上都没有真正做对。



#### v4 已知局限

1. 当前目标线稿由 COCO-17 姿态通过同一个 renderer 生成，
  因此坐标方法天然拥有正确的线宽、骨架拓扑和连接关系。
2. 这是一项“姿态渲染栅格实验”，不能直接等同于真实手绘线稿插帧。
3. `large_translation/s8` 只有 9 个验证样本，
  `large_rotation/s8` 只有 14 个，分动作结论仍需更多视频验证。
4. 同一视频中的相邻序列高度相关，1188 个序列不等于
  1188 个完全独立的动作实例。
5. 当前模型只预测一个中点，没有完成任意数量中间帧生成。



#### v4 验收结论

v4 验收通过。

同一批数据上的定量指标和并排对照图已经能够清楚说明两条路线：

- 几何/坐标表示：线条和结构稳定，但姿态或动作阶段可能错误
- 栅格表示：可能修正整体位置，但容易模糊、断线和破坏拓扑

因此不继续针对当前 RasterUNet 调参。下一步进入 v5，在统一推理
接口中优先使用多帧 Transformer，同时保留 Raster 模型作为实验
后端，并为未来的混合“姿态预测 + 图像修正”路线留下接口。

### v5 — 接进主项目

把选定的模型包成 `server/backends/stickman.py`，接口：

```python
def interpolate(prev_png: bytes, next_png: bytes, n: int) -> list[bytes]
```

跟未来的 `rife.py` / `animeinbet.py` 完全一致。

- **验收**：Krita 里点一下按钮，时间轴上的火柴人动起来。
这一步同时验证了主项目阶段 0 到阶段 3 的整条管线，
之后接 RIFE 只剩换一个文件的事

---



## 评测规矩

- **决策依据是眼睛。** 先看 `grid_*.png` 最右列的叠图（预测红、真值黑），再看数字
- **按运动类型看，不看总平均。** 总平均会把「哪类动作崩了」这个信息平均掉
- `zero_velocity` **是尺子不是对手。** 它的误差等于「这段到底动了多少」。
模型只比它好一点点 = 在抄前一帧
- `angle_lerp` **是上界不是对手。** 它用了真实线稿里拿不到的骨架信息，
代表「一个懂结构的理想方法最多能做到什么程度」。目标是从坐标/像素里逼近它
- `bone_err_px` **专抓肢体被插短。** MPJPE 有时看不出来，这个指标能

---



## 三条铁律（从 VNumber 继承）

1. **每个阶段先跑通最笨的版本**，再替换成好的。
  分不清「管线坏了」和「模型不行」是最浪费时间的状态。
2. **用自己的数据做评测，别信论文里的数字。**
3. **每次只改一个变量**，配置和产出存进 `runs/<时间戳>/`，
  这样三个月后你还能回答「当时那个效果是怎么来的」。

---



## 参考

- Harvey et al., *Robust Motion In-betweening*, SIGGRAPH 2020 — LaFAN1 数据集与基准，
公开了零速度/线性插值基线的评测代码
- Qin et al., *Motion In-betweening via Two-stage Transformers*, TOG 2022 —
代码 `victorqin/motion_inbetweening`，可以偷训练循环结构
- Siyao et al., *Deep Geometrized Cartoon Line Inbetweening*, ICCV 2023（AnimeInbet）—
v4 对照实验的动机来源
- `MarkMoHR/Awesome-2D-Animation` — 2D 动画插帧的论文与代码清单，持续更新

---



## 给 Cursor 的第一句话

> 读一下 `火柴人插帧.md`，v0 已经完成，我们从 v1 开始：
> 写 `extract_poses.py`，用 MediaPipe 从视频提 COCO-17 关键点，
> 输出成 `data/triplets/` 现有的格式。

