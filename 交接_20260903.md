# Tweener 火柴人插帧 — 交接文档（v5e 本机服务已通）

> 更新日期：2026-09-03（Asia/Shanghai）
> 项目目录：`/home/gezhuan/Desktop/YJH_Data/Projects/MovieType/Tweener`
> 本文件取代 `PROJECT_HANDOFF.md`（那份只写到 2026-08-27 上午的 v5a）和 `交接.md`（写到 v4）。
> 这两份旧文件保留作历史记录，读的时候以本文件为准。

---

## 一、新窗口的第一句话（直接复制）

> 请阅读 `/home/gezhuan/Desktop/YJH_Data/Projects/MovieType/Tweener/交接_20260903.md`，
> 以及项目根目录的 `火柴人插帧.md`，然后接着当前断点继续担任我的项目助理。
> 保持原协作方式：你负责检查、解释，并一步一步告诉我改哪个文件、代码放在哪里、运行什么命令；
> 不要直接替我实现项目代码。v0–v5e 已经完成并验收，不要重做。
> 当前断点是「PNG → 火柴人关键点」这一环不通，需要先确认结论再决定 v5f 走哪条路线。
> 先做只读核对，再只给我一个小步骤。

---

## 二、协作方式（不要改）

1. 先检查当前文件和输出，再解释结论。
2. 每次只推进一个可验证的小步骤，等我贴出运行结果再继续。
3. 明确说明代码放在哪个文件、哪个位置；较大改动给完整结构或完整文件内容。
4. 给出可直接复制的命令，并说明从哪个目录运行。
5. 除非我明确要求，不要直接替我修改项目代码。
6. 不要重复已验收的阶段，也不要因为换窗口就从头重做。

---

## 三、运行环境

```bash
cd /home/gezhuan/Desktop/YJH_Data/Projects/MovieType/Tweener
AI_PY=/home/gezhuan/Desktop/YJH_Data/Projects/Quant/yes/envs/ai/bin/python
```

- Python：上面的 `AI_PY`（3.11）
- OpenCV `4.11.0`、MediaPipe `0.10.35`、PyTorch `2.5.1+cu121`、CUDA 可用
- GPU：RTX 4090
- MediaPipe 模型：`models/pose_landmarker_full.task`

**踩过的坑**：所有 `from stickman...` / `from server...` 的程序必须从 `Tweener` 根目录运行，
在 `Tweener/stickman` 里运行会报 `ModuleNotFoundError: No module named 'stickman'`。
另外每开一个新终端都要重新 `export AI_PY=...`，否则会出现
`Command '' not found, but can be installed with: sudo apt install knot-exporter ...`
这类看起来很奇怪的报错——那只是 `$AI_PY` 为空。

---

## 四、阶段状态总表

| 阶段 | 状态 | 产出 |
| --- | --- | --- |
| v0 合成数据与传统基线 | 完成 | `zero_velocity`、LERP、角度插值；验证评测管线 |
| v1 真人视频 / 姿态提取 / 清洗 / 三连帧 | 完成 | 训练与验证视频分离；COCO-17；`s2/s4/s8` |
| v2 残差 MLP | 完成 | 三个 stride 独立模型；长间隔优于 LERP |
| v3 多帧 Transformer | 完成并验收 | 左右各 3 帧，一个模型覆盖三个 stride |
| v4 Raster U-Net | 完成并验收 | 统一图像评测、案例诊断、可视化 |
| v5a 高层 Python API | 完成 | `stickman/interpolator.py` |
| v5b 单请求 CLI | 完成 | `infer_stickman.py` |
| v5c 结构化请求契约 | 完成 | `stickman/request.py` |
| v5d 批量 CLI + 序列动画 + 诊断 | 完成 | `infer_stickman_batch.py`、`assemble_stickman_video.py`、`compare_stickman_sequence.py` |
| v5e 本机后端 + HTTP 服务 | 完成 | `server/backends/stickman.py`、`server/app.py` |
| **PNG → 关键点** | **不通（当前断点）** | MediaPipe 认真人不认火柴人线稿 |
| v5f Krita 集成 | 未开始 | `Krita_app/` 里只下载了 AppImage，没有插件代码 |

---

## 五、v0–v4 的正式结论（只读，不要重训）

### 数据

- 五种动作前缀与运动类型：`walk=medium_cyclic`、`wave=small_local`、
  `swing=large_rotation`、`squat=large_translation`、`lean=global_rotation`
- 训练视频与验证视频是分别录制的独立视频（`*_train_001.mp4` / `*_val_001.mp4`）
- 上下文 `k=3`（左右各 3 帧，共 6 个姿态 token）；时间偏移：
  - `s2`：`[-4, -3, -2, +2, +3, +4]`
  - `s4`：`[-6, -5, -4, +4, +5, +6]`
  - `s8`：`[-10, -9, -8, +8, +9, +10]`
- 样本数：train `938/448/202 = 1588`，val `698/336/154 = 1188`
- 注意：val 的 `squat/s8` 只有 9 个、`swing/s8` 只有 14 个样本，细分结论不可靠

### v3 Transformer（当前正式模型）

`d_model=96`、`heads=4`、`layers=2`、`ff=192`、`dropout=0.1`、参数 `166,018`，
预测相对 LERP 的残差，输出层零初始化。

| stride | LERP MPJPE | MLP MPJPE | Transformer MPJPE | LERP 骨长 | Transformer 骨长 |
| --- | --- | --- | --- | --- | --- |
| s2 | 0.4435 | 0.4408 | 0.4459 | 0.3212 | 0.3159 |
| s4 | 0.9415 | 0.9041 | 0.8836 | 0.6013 | 0.5677 |
| s8 | 2.0698 | 1.8531 | 1.7687 | 1.0548 | 0.9388 |

宏平均相对 LERP：MPJPE 改善 `10.32%`，骨长误差改善 `7.84%`。
收益集中在 `s4/s8`；`s2` 没有稳定超过 LERP。

### v4 Raster U-Net（保留为实验后端）

输入 6 张 mask + 6 张时间图（`12×256×256`），输出 `1×256×256`，参数 `489,297`。
Hard Dice（stride 宏平均）：`pixel_lerp 0.7102`、`pose_lerp 0.8381`、
`transformer 0.8482`、`raster_best 0.7914`、`raster_last 0.8125`。

**结论**：几何/坐标路线线条与拓扑稳定但可能算错动作相位；栅格路线偶尔能修正整体位置，
但会断头、重影、破坏拓扑。所以 v5 的推理接口默认用 Transformer，Raster 只作实验后端。
注意 v4 的目标图全部由同一个 COCO-17 渲染器生成，坐标路线天然占优，这些数字不能外推到真实手绘线稿。

---

## 六、v5 已完成的部分（2026-08-27 一天做完，按时间顺序）

所有命令都从 `Tweener` 根目录运行。下面每一条都已经实际跑通过，可以直接重放核对。

### 1. 高层 API — `stickman/interpolator.py`

`StickmanInterpolator` 封装 `SequencePredictor`，暴露
`epoch`、`context`、`strides`、`device`，以及
`predict_midpoint(...)` 和 `render_midpoint(...)`（返回 `(17,2)` 姿态 + PIL 图）。
产物：`runs/v5_smoke/`。

### 2. 结构化请求契约 — `stickman/request.py`

`SCHEMA_VERSION = 1`。`parse_request(dict)` 和 `load_request(path)` 会强校验：

- `poses.left/right` 形状必须是 `(context, 17, 2)`，可选 `poses.target` 为 `(17, 2)`
- `time_offsets.left` 全部 `< 0` 且严格递增，`right` 全部 `> 0` 且严格递增
- `left_offsets[-1] == -stride`、`right_offsets[0] == stride`
- 所有姿态必须是有限值
- `request_id` 默认取 `meta.json` 的父目录名，或文件名 stem

这个模块是 CLI、批量、后端、HTTP 服务共用的唯一入口，**不要再复制第二套归一化或校验规则**。
数据集里现成的 `meta.json` 和手写的 `request.json` 都能被它读。

### 3. 单请求 CLI — `infer_stickman.py`

```bash
"$AI_PY" infer_stickman.py \
  data/val_sequences/swing_val_001/k3/s8/swing_val_001_s8_k3_0144/meta.json \
  --checkpoint runs/formal_sequence/sequence_transformer_k3/20260816_182502/best.pt \
  --device cuda --out runs/v5_cli_smoke
```

输出 `prediction.png`、`target.png`（有真值时）、`prediction_pose.npy`、`result.json`。
`run_request(args, interpolator=None)` 被批量工具复用，模型只加载一次。

已验证的产物：`runs/v5_cli_smoke/`、`runs/v5_cli_all/{s2,s4,s8}/`、
`runs/v5_minimal_request/`（手写最小请求）、`runs/v5_target_free_smoke/`（无真值也能跑）、
`runs/v5_refactor_smoke/`、`runs/v5_shared_request_smoke/`。

⚠️ `runs/v5_cli_all/*/result.json` 里的字段名是重构**之前**的
（`input_meta` / `clip`），现在的代码写的是 `input_request` / `request_id`。
重跑会得到新字段名，这是预期的，不是 bug。

### 4. 批量 CLI — `infer_stickman_batch.py`

读 `manifest.json`（`schema_version`、`batch_id`、`requests` 为相对路径列表），
一次加载模型跑完整批，输出到 `<out>/<request_id>/`，汇总写 `batch_result.json`。
会拒绝绝对路径、重复路径、重复 `request_id`、含路径分隔符的 `request_id`。

```bash
"$AI_PY" infer_stickman_batch.py runs/v5_batch_smoke/manifest.json \
  --checkpoint runs/formal_sequence/sequence_transformer_k3/20260816_182502/best.pt \
  --device cuda --out runs/v5_batch_smoke/output
```

产物：`runs/v5_batch_smoke/`、`runs/v5_shared_batch_smoke/`。

### 5. 序列装配 — `assemble_stickman_video.py`

按 manifest 顺序把 `prediction.png` 拼成 `prediction.mp4` + `prediction.gif`，
写 `sequence_result.json`；`fps` 默认取 manifest 的 `output_fps`。
产物：`runs/v5_sequence_smoke/video/`。

### 6. 序列诊断 — `compare_stickman_sequence.py`

生成「预测 / 真值 / 叠图（预测红、真值黑）」三联动画 + 逐帧 `metrics.csv`。

`runs/v5_sequence_smoke/comparison/comparison_result.json` 的实测结果
（`walk_val_001`，`s4`，24 帧，7.5 fps）：

- 平均 MPJPE `0.4087 px`
- 平均骨长误差 `0.2371 px`
- 最差帧 `walk_s4_t0088`，MPJPE `0.8347 px`

这是目前**唯一一个连续多帧、可视化确认过的端到端结果**，走路序列上肉眼看不出明显问题。

### 7. 本机后端 — `server/backends/stickman.py`

`StickmanBackend` 在内存里完成插帧，返回 `StickmanResult`
（`pose` + `png` bytes + `metadata()`），并额外检查姿态形状、有限性和 PNG magic number。
`interpolate_midpoint(request_dict)` 和 `interpolate_many(list_of_requests)` 复用同一个模型。
产物：`runs/v5_backend_smoke/summary.json`（`model_reused: true`）。

```bash
"$AI_PY" smoke_stickman_backend.py
```

### 8. HTTP 服务 — `server/app.py`

标准库 `http.server`，默认只监听 `127.0.0.1:8765`（**不要改成 0.0.0.0**），
正文上限 16 MB，PNG 用 Base64 传回。

- `GET /health`
- `POST /v1/stickman/interpolate`
- `POST /v1/stickman/interpolate-batch`

参数错误返回 400 `invalid_request`，内部错误返回 500 且不泄漏堆栈（只打到服务端终端）。

```bash
# 终端 A
"$AI_PY" -m server.app --device cuda

# 终端 B
"$AI_PY" smoke_stickman_http.py
"$AI_PY" smoke_stickman_http_batch.py
```

两个烟雾测试都会把 HTTP 结果和本地 CLI 参考产物做**逐字节比对**
（姿态 `np.array_equal`、PNG bytes 相等），已通过。
产物：`runs/v5_http_smoke/`、`runs/v5_http_batch_smoke/`、`runs/v5_http_single_regression/`。

**结论：从「结构化姿态请求」到「PNG 结果」的整条服务链路已经可用且可复现。**

---

## 七、当前断点：PNG → 火柴人关键点不通

`stickman/pose_extractor.py` 已经写好可复用的单图提取器：
`PoseExtractor.detect_rgb / detect_pil / detect_path`，
内部把 MediaPipe-33 映射成 COCO-17，并按「补边成正方形」的方式归一化，
与训练时的坐标约定一致；检测失败返回 `None`，成功返回 `(pose, confidence)`。

`smoke_pose_extractor.py` 用它同时测真人视频帧和我们自己渲染的火柴人 PNG。
`runs/v5_pose_extractor_smoke/` 里只有 `real_frame.jpg` 和 `real_detected.png`，
**没有 `stickman_detected.png`**——而脚本只在检测成功时才保存火柴人预览。
据此推断：**MediaPipe 能识别真人帧，但识别不了当前的火柴人线稿。**

新窗口第一步应该重跑一次并把终端输出留档确认：

```bash
"$AI_PY" smoke_pose_extractor.py 2>&1 | tee runs/v5_pose_extractor_smoke/log.txt
```

### 为什么这件事卡住了整个 v5 验收

`火柴人插帧.md` 里 v5 的目标接口是：

```python
def interpolate(prev_png: bytes, next_png: bytes, n: int) -> list[bytes]
```

现在有三处不匹配，必须先做决策，不要闷头实现：

1. **输入形式不匹配**：模型要左右各 3 帧姿态，不是两张 PNG。
2. **PNG 无法转成姿态**：MediaPipe 不认火柴人线稿（本节的结论）。
3. **时间点不匹配**：模型只预测正中间一帧，且只训练过 stride `2/4/8`，
   没有任意 `n` 和连续时间参数。

---

## 八、下一步的三条候选路线

### 路线 A：先做 Krita 最小闭环，绕开 PNG → 姿态（推荐）

让 Krita 插件传结构化姿态（从现成 `.npz` / JSON 读，或插件里手动标点），
调用已经跑通的 HTTP 服务，把返回的 PNG 贴回时间轴。

- 优点：能最快验证主项目阶段 0–3 的整条管线，风险最低，服务端已经完全就绪
- 代价：暂时不满足「任意 PNG 输入」这个最终目标
- 建议的第一小步：安装 `Krita_app/krita-5.3.3-x86_64.AppImage`，
  建一个只做「调用 `GET /health` 并弹窗显示 epoch / device / strides」的插件骨架，
  先确认 Krita 的 Python 环境能访问本机 8765 端口

### 路线 B：先解决 PNG → 关键点

用我们自己的渲染器批量生成「渲染图 → 17 关键点」的监督数据，
训一个小的关键点回归/热图网络，替掉这一环的 MediaPipe。

- 优点：这是「任意 PNG 输入」的必要条件，也更接近真实手绘线稿的最终场景
- 代价：是一个新的训练任务，不是几步就能收尾
- 风险：训练数据来自同一个渲染器，同样有「拓扑和线宽一致性」的偏差问题，v4 已经踩过

### 路线 C：先解决任意 `n` 与任意时间

让模型支持连续时间参数或递归补帧（原计划的 v5c）。

- 优点：直接对齐目标接口的 `n`
- 代价：要重训 Transformer 或引入递归，还要评估姿态漂移累积
- 现在的模型是「只预测中点 + 只有三个 stride」，硬套 `n` 会静默失真

**建议**：先按路线 A 拿到端到端闭环（能在 Krita 里看到火柴人动起来），
把路线 B 当作并行的独立研究任务，路线 C 放到最后。

---

## 九、不要丢失的正式产物

- Transformer 正式 checkpoint：`runs/formal_sequence/sequence_transformer_k3/20260816_182502/best.pt`（epoch 21）
- Transformer 统一评测：`runs/formal_sequence_eval/20260816_183158`
- MLP 正式 checkpoint：`runs/formal_mlp/mlp_s{2,4,8}/...`
- Raster 正式训练：`runs/formal_raster/raster_unet_k3/20260826_164507`（`best.pt` epoch 7、`last.pt` epoch 47）
- Raster 正式评测与案例：`runs/formal_raster_eval/20260826_172117`
- v5 全部烟雾测试：`runs/v5_*`（15 个目录，见第六节）
- 数据：`data/videos`、`data/poses`、`data/formal_sequences`、`data/val_sequences`
- MediaPipe 模型：`models/pose_landmarker_full.task`

---

## 十、只读核对命令（新窗口先跑这些）

```bash
cd /home/gezhuan/Desktop/YJH_Data/Projects/MovieType/Tweener
export AI_PY=/home/gezhuan/Desktop/YJH_Data/Projects/Quant/yes/envs/ai/bin/python

# 1. 关键文件在不在
ls -lh \
  infer_stickman.py infer_stickman_batch.py \
  assemble_stickman_video.py compare_stickman_sequence.py \
  stickman/request.py stickman/interpolator.py stickman/pose_extractor.py \
  server/app.py server/backends/stickman.py \
  models/pose_landmarker_full.task \
  runs/formal_sequence/sequence_transformer_k3/20260816_182502/best.pt

# 2. 语法检查（2026-09-03 全部通过）
"$AI_PY" -m py_compile \
  infer_stickman.py infer_stickman_batch.py \
  assemble_stickman_video.py compare_stickman_sequence.py \
  server/app.py server/backends/stickman.py \
  stickman/request.py stickman/pose_extractor.py stickman/interpolator.py

# 3. 端到端最快复核：启动服务后在另一个终端跑
"$AI_PY" -m server.app --device cuda
"$AI_PY" smoke_stickman_http.py && "$AI_PY" smoke_stickman_http_batch.py
```

---

## 十一、关键文件索引

### 说明文档

- `火柴人插帧.md`：主路线与各阶段设计（写到 v4 验收 + v5 计划）
- `交接_20260903.md`：本文件，当前唯一有效交接
- `PROJECT_HANDOFF.md`：历史交接（截至 v5a），细节仍可查
- `交接.md`：更早的历史交接（截至 v4）

### 数据处理

`extract_poses.py`、`make_triplets.py`、`make_real_triplets.py`、`make_real_sequences.py`

### 姿态模型

`train.py`、`train_sequence.py`、`evaluate.py`、`evaluate_sequence.py`、
`visualize_sequence_eval.py`、`stickman/dataset.py`、`stickman/sequence_dataset.py`、
`stickman/losses.py`、`stickman/baselines.py`、`stickman/models/{mlp,predictor,seq,sequence_predictor}.py`

### Raster 模型与诊断

`train_raster.py`、`evaluate_raster.py`、`summarize_raster_eval.py`、`select_raster_cases.py`、
`visualize_raster_checkpoint.py`、`visualize_raster_selection.py`、
`stickman/raster_dataset.py`、`stickman/raster_losses.py`、`stickman/models/unet.py`

### v5 推理与服务

`stickman/interpolator.py`、`stickman/request.py`、`stickman/pose_extractor.py`、
`stickman/skeleton.py`（`render` / `render_overlay` / `mpjpe` / `bone_length_error`，`CANVAS=256`）、
`infer_stickman.py`、`infer_stickman_batch.py`、
`assemble_stickman_video.py`、`compare_stickman_sequence.py`、
`server/app.py`、`server/backends/stickman.py`

### 烟雾测试

`smoke_stickman_backend.py`、`smoke_stickman_http.py`、`smoke_stickman_http_batch.py`、
`smoke_pose_extractor.py`

### 其他

`stickman/motion.py`：v0 的合成动作生成器，**只用于调试管线，不要用来训练**
`Krita_app/krita-5.3.3-x86_64.AppImage`：已下载未安装，插件代码还没有

---

## 十二、已知小问题与待办（都不阻塞下一步）

- `make_real_triplets.py` 在完全缺失的相邻帧上会有 `All-NaN slice encountered` 警告；
  过滤结果正确，但应给 `np.nanmax` 加「是否存在有限值」的保护
- `runs/v5_cli_all/*/result.json` 是重构前的旧字段名（见第六节第 3 条）
- 项目**没有任何单元测试**，只有 `smoke_*.py`；应补最小测试：
  checkpoint 兼容性、shape、finite、确定性、错误输入
- 项目不是 git 仓库，没有版本历史，全靠 `runs/<时间戳>/` 留痕
- 数据量仍小，每个运动类型只有一段训练视频和一段验证视频
- Transformer 在 `s2` 和部分 `small_local` 样本没有稳定超过 LERP，
  可考虑 stride gating 或短间隔直接回退 LERP
- Raster 的训练选择指标与最终 hard Dice / 视觉质量不一致；若重训应分别保存
  `best_total_loss.pt`、`best_hard_dice.pt`、`last.pt`
- 目前所有目标图都来自同一个 COCO-17 渲染器，不等价于真实手绘线稿

---

## 十三、交接完成标准

新窗口的助理做完这三件事就算正确接上：

1. 读完本文件和 `火柴人插帧.md`，确认不重做 v0–v5e。
2. 跑完第十节的只读核对，确认服务链路仍然可用。
3. 重跑 `smoke_pose_extractor.py` 确认第七节的结论，
   然后就第八节的 A/B/C 路线跟我确认一个方向，再给出第一个小步骤。
